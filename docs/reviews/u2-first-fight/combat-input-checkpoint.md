# Combat input checkpoint

Runtime/test source: **a40902a0ef7e0d9cb2c40d8fc0be400b43e80a90**. This checkpoint adds Unity's combat wire decoder and touch attack input to the existing Emberworks encounter. It does not yet provide a visible, playable fight.

The v4 decoder now retains the server's hero dictionary, enemy identities/states and combat events. A snapshot generated by the real Node simulation and protocol writer exercises both heroes independently. Newtonsoft 3.2.2 was already a transitive Unity dependency; it is now explicit, with narrow IL2CPP preservation for the wire DTOs. A new WebGL build is still required to verify that runtime path.

Attack has a sequence independent of movement, and disconnect clears the joined identity before controls can send again. The lower-right button reserves its originating finger even if it drags outside the button. Movement and camera fingers keep their own ownership. A local button pulse acknowledges a press; the server still owns contact and damage. The button supports Space and clicking for desktop testing.

At the source SHA above:

- **60 EditMode tests pass** across connection, movement/camera and new combat protocol cases.
- **5 PlayMode tests pass**, including an actual three-touch input sequence with movement, attack and orbit together. The optional custody-tier asset test is skipped in this ordinary headless suite.
- The candidate's separate graphics-enabled **Unity PlayMode diagnostic passed at aed840116cfd6dfbfec83ba549fd87dfd238cc6b**; see [rig review](gremlin-local-rig-v1.md).
- The full local Node suite at the preceding runtime-identical commit **b2e36077f7cec2dfc95069b073a74c37a1186626** reports **2,283 pass, 1 fail, 3 skip**. The remaining failure is the previously reproduced `lantern-xp-award.test.mjs` Windows `rmSync` temporary-folder cleanup `EPERM`. The only intervening change isolates a Unity camera test fixture.

The broader Unity run caught an existing order dependency: connection tests left Emberworks active, and camera fixtures added their test camera to that same scene before querying the real camera. Creating an empty fixture scene fixed the two observed failures without changing runtime camera behavior.

Still required for M2: the enemy presenter and staged candidate in the running Unity game, hero attack/reaction animations, health/telegraph feedback, audio, two-client battle/recovery, exact-source WebGL verification, physical Safari and Owner visual/promotion decisions. No browser or physical-device acceptance is claimed here.
